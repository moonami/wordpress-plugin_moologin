<div class="wrap">    <h2><?php print MOOLOGIN_PUGIN_NAME ." ". MOOLOGIN_CURRENT_VERSION ?></h2>    <p>This plugin allows you to place a shortcode in a post that passes encrypted logon information to Moodle. <br />    The user will be created (if required) by Moodle and enrolled in the specified Course or Cohort.<br />     Use the Moodle button on the rich editor to insert the shortcode.</p>    <p>Simple example: <em>[moologin]link text[/moologin]</em>.</p>    <p>Full example: <em>[moologin courseid="3" class="css-classname" cohort="class1" target="_blank"]link text[/moologin]</em>.</p>    <h2>Shortcode attributes:</h2>    <ul>        <li><em>class</em>: optional, defaults to 'moologin'; class attribute of link</li>        <li><em>courseid</em>: optional, value of the courseid in which to enrol the user <span style="font-weight:bold;">(Course must have self enrolment instance named "moologin" enabled!!!)</span></li>        <li><em>cohort</em>: optional, name of the cohort in which to enrol a user at the moodle end</li>        <li><em>target</em>: optional, defaults to '_self'; target attribute of link</li>    </ul>    <p class="description">Note: The link that is generated is timestamped and will expire, so it cannot be bookmarked or hijacked.<br />    You must set the expiry time in the Moodle plugin. You should allow reading time of the page when considering a timeout value, since the link is generated when the page is loaded, not when the link is clicked.</p>    <h2>Settings</h2>    <form method="post" action="options.php">        <?php            settings_fields( 'moologin-settings-group' );        ?>        <table class="form-table">            <tr valign="top">                <th scope="row">Moodle URL</th>                <td>                    <input type="text" name="moologin_moodle_url" value="<?php echo get_option('moologin_moodle_url'); ?>" size="60" />                    <span class="description">Plugin will append the url <em><?php echo MOOLOGIN_MOODLE_PLUGIN_URL ?></em>.</span>                </td>            </tr>            <tr valign="top">                <th scope="row">Encryption secret</th>                <td>                    <input type="text" name="moologin_shared_secret" value="<?php echo get_option('moologin_shared_secret'); ?>" size="60" />                    <span class="description">Must match setting on Moodle plugin</span>                </td>            </tr>        </table>        <p class="submit">            <input type="submit" class="button-primary" value="<?php _e('Save Changes') ?>" />        </p>    </form></div>